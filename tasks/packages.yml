---

- name: Install package
  hosts: localhost
  connection: local
  become: true

  tasks:
    - name: Install packages
      apt:
        name:
          - tmux
          - htop
        state: latest
        